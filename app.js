const spotBtn = document.querySelector('.spotBtn');

spotBtn.addEventListener('mouseover', (height) => {
    spotBtn.style.height = "20px";
})

spotBtn.addEventListener('mouseout', (stop) => {
    spotBtn.style.height = "16px";
})

const instaBtn = document.querySelector('.instaBtn');

instaBtn.addEventListener('mouseover', (height) => {
    instaBtn.style.height = "20px";
})

instaBtn.addEventListener('mouseout', (stop) => {
    instaBtn.style.height = "16px";
})

const linkedBtn = document.querySelector('.linkInBtn');

linkedBtn.addEventListener('mouseover', (height) => {
   linkedBtn.style.height = "20px";
})

linkedBtn.addEventListener('mouseout', (stop) => {
    linkedBtn.style.height = "16px";
})